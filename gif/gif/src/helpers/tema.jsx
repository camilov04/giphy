import { createTheme } from '@mui/material/styles';

export const tema = createTheme({
    palette: {
      primary: {
        main: '#004d40',
      },
      secondary: {
        main: '#FFFFFF',
      },
    },
  });
